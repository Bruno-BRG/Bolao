export default function Loading() {
  return (
    <main className="container">
      <section className="card" style={{ padding: "2rem", minHeight: "10rem" }}>
        <p className="muted">Carregando...</p>
      </section>
    </main>
  );
}
